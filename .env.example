# =============================================================================
# .env.example — copiare in .env e valorizzare (il .env NON va in git)
#   cp .env.example .env
# Uso locale:  python manage.py runserver
# Uso Docker:  docker compose up -d --build   (DB_HOST=postgres)
# =============================================================================

# --- Database PostgreSQL (OBBLIGATORIO) ---
DB_NAME=carico_3d
DB_USER=carico
DB_PASSWORD=cambiami
DB_HOST=localhost            # con Docker Compose: postgres
DB_PORT=5432

# --- Django (OBBLIGATORIO) ---
SECRET_KEY=cambiami-con-una-chiave-forte-aleatoria
DEBUG=False                  # in sviluppo: True
ALLOWED_HOSTS=localhost,127.0.0.1,tuodominio.com

# --- HTTPS / CSRF (produzione) ---
CSRF_TRUSTED_ORIGINS=https://tuodominio.com
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://tuodominio.com
SECURE_SSL_REDIRECT=False    # True in produzione (o redirect in nginx)
SESSION_COOKIE_SECURE=False  # True in produzione (HTTPS)
CSRF_COOKIE_SECURE=False     # True in produzione (HTTPS)
SECURE_HSTS_SECONDS=0         # 31536000 solo dopo aver verificato HTTPS stabile
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
# Proxy fidati da cui accettare X-Real-IP (IP/CIDR separati da virgola).
# Vuoto = fidati solo i peer privati/loopback (nginx sul bridge Docker).
TRUSTED_PROXY_IPS=
LOG_LEVEL=INFO

# --- Fungies.io — Pagamenti (Merchant of Record) ---
# Creare su Fungies due subscription offer (mensile/annuale) e due Checkout
# Element hosted, poi copiare gli UUID. Il checkout è ospitato da Fungies:
# i dati della carta non passano dal backend Carico 3D.
FUNGIES_STORE_URL=https://tuostore.fungies.io
FUNGIES_PORTAL_URL=https://tuostore.fungies.io/portal  # opzionale: default STORE_URL/portal
FUNGIES_OFFER_MENSILE_ID=
FUNGIES_OFFER_ANNUALE_ID=
FUNGIES_CHECKOUT_ELEMENT_MENSILE_ID=
FUNGIES_CHECKOUT_ELEMENT_ANNUALE_ID=
FUNGIES_WEBHOOK_SECRET=

# --- Email transazionali (Resend) ---
# Obbligatorio per l'invio reale delle email di verifica account.
# Senza RESEND_API_KEY il backend cade su console (log container, niente invio).
# RESEND_FROM: usare l'indirizzo del dominio verificato su resend.com
# (es. "Carico 3D <noreply@carico3d.com>"). Finché il dominio non è
# verificato, Resend accetta solo onboarding@resend.dev.
RESEND_API_KEY=
RESEND_FROM=Carico 3D <noreply@carico3d.com>
# DEFAULT_FROM_EMAIL=  # opzionale: ricade su RESEND_FROM

# --- Icone (solo Docker: percorsi dei volumi, impostati nel compose) ---
# ICON_CONFIG_PATH=/data/icon_config.json
# ICON_UPLOAD_DIR=/data/img_uploads

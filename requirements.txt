# Django web framework
Django==5.2.1

# Django REST Framework (API)
djangorestframework==3.16.1

# CORS headers per sviluppo frontend
django-cors-headers==4.9.0

# Coda task asincroni (Django Q2)
django-q2>=1.7.0

# Database SQL parser (dipendenza di Django)
sqlparse==0.5.3

# Autenticazione social (Google OAuth2)
django-allauth>=0.63.0

# Dipendenze django-allauth
PyJWT>=2.0
cryptography>=3.4
requests>=2.25

# Fingerprinting browser (opzionale, per demo abuse prevention)
# fingerprintjs è caricato via CDN nel frontend

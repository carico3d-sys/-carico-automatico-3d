# Django web framework
Django==6.0.7

# Django REST Framework (API)
djangorestframework==3.17.1

# CORS headers
django-cors-headers==4.9.0

# Coda task asincroni (Django Q2)
django-q2==1.10.0

# Protezione brute-force sul login
django-axes==8.3.1

# Database SQL parser (dipendenza di Django)
sqlparse==0.5.5

# PostgreSQL adapter
psycopg2-binary==2.9.12

# Variabili d'ambiente da file .env
python-dotenv==1.2.2

# Autenticazione social (Google OAuth2)
django-allauth==65.18.0

# Dipendenze django-allauth
PyJWT==2.13.0
cryptography==50.0.0
requests==2.34.2

# WSGI server per la produzione
gunicorn==23.0.0

# Serving dei file statici con Gunicorn/Render
whitenoise==6.12.0

# Fingerprinting browser (opzionale, per demo abuse prevention)
# fingerprintjs è caricato via CDN nel frontend
